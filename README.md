# live-polling-voting-system
Real-time live polling system built using Python and UDP socket programming. Multiple clients send votes to a central server using their SRN as a unique voter ID. The server validates votes, prevents duplicates, aggregates results, and demonstrates packet transmission that can be analyzed using
